# Kernel Walkthrough
Given a Triton kernel file, produce a markdown doc with: 1) high-level intent, 2) tile/block hierarchy, 3) outer loop structure, 4) memory access pattern, 5) ASCII visualization. Write to ./docs/walkthroughs/<kernel_name>.md unless user specifies otherwise.
